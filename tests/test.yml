---
- hosts: travis
  become: yes
  tasks:
  - pip:
      name: setuptools
      version: 32.3.1
  - pip:
      name: docker-py
      version: 1.10.6

- hosts: all
  become: yes
  roles:
    - role: ../..
      nginx_files_local_folder: './files'
      nginx_configuration_files: ['default.conf']
